% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{run_germline_gene_scoring}
\alias{run_germline_gene_scoring}
\title{Run MAGMA germline gene scoring}
\usage{
run_germline_gene_scoring(
  gwas_sumstats,
  gene_loc_path = NULL,
  reference_bfile,
  output_prefix = NULL,
  sample_size = NULL,
  sample_size_col = NULL,
  magma_path = NULL,
  magma_gwas_cache_prefix = NULL,
  reuse_existing_gwas_cache = TRUE,
  reuse_existing_annotation = FALSE,
  reuse_existing_analysis = FALSE,
  keep_intermediates = FALSE,
  step1_args = list(),
  step2_args = list(),
  verbose = FALSE
)
}
\arguments{
\item{gwas_sumstats}{GWAS summary statistics path or table.}

\item{gene_loc_path}{Optional gene location file for MAGMA step 1. When
\code{NULL}, \code{conseguiR} uses its backend gene location resource.}

\item{reference_bfile}{PLINK reference prefix for MAGMA step 2.}

\item{output_prefix}{Output prefix for saved artifacts.}

\item{sample_size}{Fixed sample size for MAGMA.}

\item{sample_size_col}{Optional sample size column name.}

\item{magma_path}{Optional path to the MAGMA executable. When \code{NULL},
\code{conseguiR} searches in this order: \code{options(conseguiR.magma_path = ...)},
\code{Sys.getenv("CONSEGUIR_MAGMA_PATH")}, then \code{magma} on \code{PATH}.}

\item{magma_gwas_cache_prefix}{Optional shared MAGMA GWAS cache prefix.}

\item{reuse_existing_gwas_cache}{Whether to reuse the shared MAGMA cache.}

\item{reuse_existing_annotation}{Whether to reuse an existing MAGMA
annotation output.}

\item{reuse_existing_analysis}{Whether to reuse an existing MAGMA gene
analysis output.}

\item{keep_intermediates}{Whether to keep intermediate MAGMA files.}

\item{step1_args}{Named list of MAGMA step 1 arguments. Supported entries
include \code{annotation_window}, \code{filter_path}, \code{ignore_strand}, \code{nonhuman},
and \code{extra_args}.}

\item{step2_args}{Named list of MAGMA step 2 arguments. Supported entries
include \code{gene_model}, \code{genes_only}, \code{pval_use}, \code{pval_duplicate},
\code{bfile_synonyms}, \code{bfile_synonym_dup}, and \code{extra_args}.}

\item{verbose}{Logical scalar. If \code{TRUE}, show a progress bar and MAGMA step
output.}
}
\value{
A germline gene score bundle.
}
\description{
This wrapper exposes both MAGMA stages for the gene-level run:
}
\details{
\itemize{
\item step 1: annotation
\item step 2: gene analysis
}

Pass stage-specific MAGMA arguments through \code{step1_args} and \code{step2_args}.
If \code{gene_loc_path} is \code{NULL}, the package will try to use the backend-owned
gene location resource shipped with \code{conseguiR}.

Exact formatting:
\itemize{
\item \code{gwas_sumstats}: either a file path or a data frame/data.table with GWAS
columns.
\item \code{gene_loc_path}: a single path to a MAGMA-compatible gene location file.
\item \code{reference_bfile}: the shared PLINK reference prefix, typed without file
suffixes, for example \verb{\\"/path/to/g1000_eur/g1000_eur\\"}.
\item \code{output_prefix}: a single path prefix, not a directory. For example
\verb{\\"results/germline_gene\\"} will yield MAGMA files such as
\code{results/germline_gene.genes.out}.
\item \code{sample_size}: use this when the full GWAS should be analyzed with one
fixed sample size.
\item \code{sample_size_col}: use this when the GWAS table already contains a sample
size column that MAGMA should read per row instead.
\item \code{sample_size} and \code{sample_size_col} are alternatives. In a typical run you
use one or the other, not both.
\item \code{step1_args} and \code{step2_args}: named lists. For example:
}

\code{step1_args = list( annotation_window = c(35, 10), filter_path = NULL, ignore_strand = FALSE, nonhuman = FALSE, extra_args = character() )}

\verb{step2_args = list( gene_model = \\"snp-wise=mean\\", genes_only = TRUE, pval_use = c(\\"SNP\\", \\"P\\"), pval_duplicate = \\"drop\\", bfile_synonyms = NULL, bfile_synonym_dup = NULL, extra_args = character() )}

The most common wrapper-level MAGMA inputs usually look like this:
\itemize{
\item \code{reference_bfile = "/path/to/g1000_eur/g1000_eur"}
\item \code{sample_size = 456348}
\item \code{sample_size_col = "n_total"} when the GWAS already contains a usable
sample-size column
}

In plain language:
\itemize{
\item MAGMA step 1 (\code{step1_args}) controls how SNPs are assigned to features.
The most important knob is usually \code{annotation_window}, which expands the
feature boundaries upstream and downstream before MAGMA annotates SNPs.
\item MAGMA step 2 (\code{step2_args}) controls how annotated SNP-level signal is
collapsed into one feature-level statistic. The most important knobs are
usually \code{gene_model}, \code{pval_use}, and \code{pval_duplicate}.
\item \code{pval_use = c("SNP", "P")} tells MAGMA which columns in the p-value file
correspond to SNP IDs and p-values.
\item \code{pval_duplicate} follows MAGMA's own duplicate-SNP handling, for example
\code{"drop"}, \code{"first"}, \code{"last"}, or \code{"error"}.
\item \code{genes_only = TRUE} is a practical default when you want the standard
feature-level MAGMA result rather than broader auxiliary outputs.
\item \code{gene_model = "snp-wise=mean"} is a common starting point because it asks
MAGMA to summarize SNP-level signal across the assigned feature rather than
relying on a single top SNP.
}

MAGMA manual:
\url{https://ibg.colorado.edu/cdrom2021/Day10-posthuma/magma_session/manual_v1.09a.pdf}
}
\examples{
names(formals(run_germline_gene_scoring))

}
