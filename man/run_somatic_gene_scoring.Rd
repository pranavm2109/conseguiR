% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{run_somatic_gene_scoring}
\alias{run_somatic_gene_scoring}
\title{Run dndscv somatic gene scoring}
\usage{
run_somatic_gene_scoring(
  maf,
  refdb,
  output_path = NULL,
  cv = NULL,
  max_muts_per_gene_per_sample = 6L,
  max_coding_muts_per_sample = 5000L,
  gene_list = NULL,
  sm = "192r_3w",
  kc = "cgc81",
  use_indel_sites = TRUE,
  min_indels = 5L,
  maxcovs = 20L,
  constrain_wnon_wspl = TRUE,
  outp = 3L,
  numcode = 1L,
  outmats = FALSE,
  mingenecovs = 500L,
  onesided = NULL,
  dc = NULL,
  dndscv_args = list(),
  verbose = FALSE
)
}
\arguments{
\item{maf}{Somatic MAF path or table.}

\item{refdb}{dndscv reference database path.}

\item{output_path}{Optional output path for saved scores.}

\item{cv}{Optional dndscv covariates.}

\item{max_muts_per_gene_per_sample}{dndscv gene-level mutation cap.}

\item{max_coding_muts_per_sample}{dndscv sample-level coding mutation cap.}

\item{gene_list}{Optional vector of genes passed to \code{dndscv()}.}

\item{sm}{dndscv substitution model.}

\item{kc}{dndscv known-cancer-gene setting.}

\item{use_indel_sites}{Whether dndscv should use indel sites.}

\item{min_indels}{Minimum number of indels for dndscv indel modeling.}

\item{maxcovs}{Maximum number of covariates passed to dndscv.}

\item{constrain_wnon_wspl}{Whether to constrain \code{wnon == wspl} in dndscv.}

\item{outp}{dndscv output level.}

\item{numcode}{Genetic code used by dndscv.}

\item{outmats}{Whether dndscv should return count/exposure matrices.}

\item{mingenecovs}{Minimum number of genes for dndscv covariate modeling.}

\item{onesided}{Optional dndscv one-sided testing switch.}

\item{dc}{Optional dndscv duplex-coverage vector.}

\item{dndscv_args}{Named list of additional dndscv arguments.}

\item{verbose}{Logical scalar. If \code{TRUE}, show stage messages and dndscv
output.}
}
\value{
A somatic gene score bundle.
}
\description{
This wrapper exposes the relevant \code{dndscv()} parameter surface used by the
package, while still allowing additional passthrough through \code{dndscv_args}.
}
\details{
Exact formatting:
\itemize{
\item \code{maf}: either a file path or a data frame/data.table containing somatic
mutation records.
\item \code{refdb}: a single path to a dndscv reference database \code{.rda} file.
\item \code{cv}: optional covariate structure passed through to \code{dndscv()}. In most
user workflows this is \code{NULL}, but advanced users can pass a covariate
matrix or object that matches dndscv's expectations.
\item \code{dndscv_args}: a named list of additional dndscv arguments, for example
\verb{list(sm = \\"192r_3w\\", kc = \\"cgc81\\")}.
\item \code{maf} should already be MAF-like before it reaches this function. In
practice that means sample, chromosome, start, end, reference allele, and
alternate allele information should already be available under standard
MAF-style column names or names the package validator can harmonize.
}

In plain language:
\itemize{
\item \code{max_muts_per_gene_per_sample} caps how many mutations from one sample can
contribute to one gene before dndscv filters them down.
\item \code{max_coding_muts_per_sample} caps extremely hypermutated samples in the
coding analysis.
\item the most common advanced dndscv passthrough is \code{sm}, which controls the
substitution model, for example \verb{\\"192r_3w\\"}.
\item by default, \code{conseguiR} asks \code{dndscv()} for one-sided positive and
negative selection tests when that API surface is available. In practice
this means the package prefers directional columns such as \code{ppos_cv} and
\code{pneg_cv} for score extraction.
\item if those one-sided columns are not present, \code{conseguiR} falls back to the
older signed two-sided extraction path so older \code{dndscv} outputs still
work.
\item if you supply \code{cv}, it should already be formatted the way \code{dndscv()}
expects it. \code{conseguiR} does not reshape arbitrary covariate tables into a
dndscv-ready object for you.
\item \code{refdb} is not just any annotation file. It must be a dndscv-compatible
reference \code{.rda} resource built for the same genome build as the MAF you
are scoring.
}

Minimal examples:

\code{dndscv_args = list()}

\code{cv = NULL}

If you do use \code{cv}, think of it as an advanced dndscv-native object rather
than a generic spreadsheet of covariates. The package passes it through; it
does not convert a plain data frame into the structure expected by dndscv.

dndscv documentation:
\url{https://rdrr.io/github/im3sanger/dndscv/man/dndscv.html}
}
\examples{
names(formals(run_somatic_gene_scoring))

}
