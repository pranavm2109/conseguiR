% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{run_gene_reg_diffusion}
\alias{run_gene_reg_diffusion}
\title{Run diffusion on a scored gene-regulatory graph}
\usage{
run_gene_reg_diffusion(
  scored_graph = NULL,
  nodes_path = NULL,
  edges_path = NULL,
  output_dir = NULL,
  output_stem = NULL,
  top_k = 3L,
  confidence_power = 2,
  beta_germline = 0.5,
  beta_somatic = 0.5,
  beta_epigenomic = 0.7,
  positive_only = FALSE,
  reg_signal_clip = 5,
  top_n_to_save = 50L,
  python_path = NULL,
  verbose = FALSE
)
}
\arguments{
\item{scored_graph}{Optional scored graph bundle.}

\item{nodes_path}{Optional explicit scored node table path.}

\item{edges_path}{Optional explicit scored edge table path.}

\item{output_dir}{Output directory for diffusion artifacts.}

\item{output_stem}{Output stem for diffusion artifacts.}

\item{top_k}{Diffusion top-k parameter.}

\item{confidence_power}{Edge-confidence exponent.}

\item{beta_germline}{Germline contribution weight.}

\item{beta_somatic}{Somatic contribution weight.}

\item{beta_epigenomic}{Epigenomic contribution weight.}

\item{positive_only}{Whether to restrict to positive regulatory signal.}

\item{reg_signal_clip}{Regulatory signal clip value.}

\item{top_n_to_save}{Number of top genes to save separately.}

\item{python_path}{Deprecated and ignored. Python is managed internally via
\code{basilisk}.}

\item{verbose}{Logical scalar. If \code{TRUE}, show stage messages while running
diffusion.}
}
\value{
A diffusion bundle containing full and top-gene diffusion tables.
}
\description{
This wrapper exposes the main hyperparameters used by the current
regulatory-to-gene diffusion implementation.
}
\details{
Exact formatting:
\itemize{
\item \code{scored_graph}: the bundle returned by \code{build_scored_gene_reg_graph()}
\item \code{nodes_path} and \code{edges_path}: paths to the scored node and edge tables if
you are not passing \code{scored_graph}
\item \code{top_k}: a positive integer
\item \code{confidence_power}, \code{beta_germline}, \code{beta_somatic},
\code{beta_epigenomic}, \code{reg_signal_clip}: numeric scalars
\item \code{positive_only}: a single logical value
}

In plain language:
\itemize{
\item \code{top_k} controls how many regulatory neighbors contribute most strongly to
each gene's update step
\item \code{beta_germline}, \code{beta_somatic}, and \code{beta_epigenomic} control how much
each modality contributes to the regulatory signal that is propagated
\item \code{confidence_power} upweights or downweights high-confidence regulatory
links relative to weaker ones
\item \code{positive_only = TRUE} suppresses negative regulatory signal before
diffusion
\item \code{reg_signal_clip} caps extreme regulatory signal before propagation so one
extreme feature does not dominate the update
}
}
\examples{
\dontrun{
scored_graph <- build_scored_gene_reg_graph(
  gene_germline_scores = "germline_gene_scores.tsv",
  reg_germline_scores = "germline_reg_scores.tsv",
  gene_somatic_scores = "somatic_gene_scores.tsv",
  reg_somatic_scores = "somatic_reg_scores.tsv",
  reg_epigenomic_scores = "epigenomic_reg_scores.tsv"
)

run_gene_reg_diffusion(
  scored_graph = scored_graph,
  top_k = 3L,
  beta_epigenomic = 0.7
)
}

}
