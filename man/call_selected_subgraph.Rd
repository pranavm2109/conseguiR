% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{call_selected_subgraph}
\alias{call_selected_subgraph}
\title{Call the selected subgraph from diffusion results}
\usage{
call_selected_subgraph(
  diffusion = NULL,
  diffusion_path = NULL,
  gg_nodes_path = NULL,
  gg_edges_path = NULL,
  output_dir = NULL,
  output_stem = NULL,
  target_genes = 50L,
  candidate_pool_size = 400L,
  min_confidence = 0,
  max_edges_in_model = 12000L,
  node_prize_weight = 1,
  edge_conf_weight = 1,
  edge_cost_weight = 1,
  node_scale = 1000L,
  edge_scale = 1000L,
  max_time_seconds = 600L,
  num_workers = 8L,
  random_seed = 42L,
  prize_column = "post_norm",
  confidence_column = "confidence",
  edge_cost_column = "weight",
  python_path = NULL,
  verbose = FALSE
)
}
\arguments{
\item{diffusion}{Optional diffusion bundle.}

\item{diffusion_path}{Optional explicit diffusion table path.}

\item{gg_nodes_path}{Gene-gene node table path.}

\item{gg_edges_path}{Gene-gene edge table path.}

\item{output_dir}{Output directory for selected-subgraph artifacts.}

\item{output_stem}{Output stem for selected-subgraph artifacts.}

\item{target_genes}{Requested number of genes in the selected subgraph.}

\item{candidate_pool_size}{Candidate pool size for the solver.}

\item{min_confidence}{Minimum edge confidence allowed into the solver.}

\item{max_edges_in_model}{Maximum number of edges in the optimization model.}

\item{node_prize_weight}{Node prize weight.}

\item{edge_conf_weight}{Edge confidence weight.}

\item{edge_cost_weight}{Edge cost weight.}

\item{node_scale}{Integer scaling factor for node prizes.}

\item{edge_scale}{Integer scaling factor for edge terms.}

\item{max_time_seconds}{Solver time limit in seconds.}

\item{num_workers}{Solver worker count.}

\item{random_seed}{Solver random seed.}

\item{prize_column}{Diffusion prize column to optimize.}

\item{confidence_column}{Gene-gene confidence column.}

\item{edge_cost_column}{Gene-gene edge-cost column.}

\item{python_path}{Deprecated and ignored. Python is managed internally via
\code{basilisk}.}

\item{verbose}{Logical scalar. If \code{TRUE}, show stage messages while calling
the selected subgraph.}
}
\value{
A selected subgraph bundle.
}
\description{
This wrapper exposes the main candidate-selection, objective-weighting, and
solver controls used by the current subgraph-calling implementation.
}
\details{
Exact formatting:
\itemize{
\item \code{diffusion}: the bundle returned by \code{run_gene_reg_diffusion()}
\item \code{diffusion_path}: path to the full diffusion results table if \code{diffusion}
is not supplied
\item \code{target_genes}, \code{candidate_pool_size}, \code{max_edges_in_model},
\code{max_time_seconds}, \code{num_workers}, \code{random_seed}: integer scalars
\item \code{node_prize_weight}, \code{edge_conf_weight}, \code{edge_cost_weight}: numeric
scalars
\item \code{prize_column}, \code{confidence_column}, \code{edge_cost_column}: single column
names present in the input tables
}

Practical column examples:
\itemize{
\item \code{prize_column = "post_norm"} means optimize using the post-diffusion gene
score column
\item \code{confidence_column = "confidence"} means use the confidence column from
the backend gene-gene edge table
\item \code{edge_cost_column = "weight"} means use the edge-cost/penalty column from
the backend gene-gene edge table
}

In plain language:
\itemize{
\item \code{target_genes} is the size of the final selected subgraph you want back
\item \code{candidate_pool_size} is the number of top candidate genes that are handed
to the optimization stage before the final smaller subgraph is chosen
\item \code{node_prize_weight} rewards high-scoring genes from diffusion
\item \code{edge_conf_weight} rewards keeping confident gene-gene edges
\item \code{edge_cost_weight} penalizes expensive edges
\item \code{max_edges_in_model} and \code{max_time_seconds} are practical solver-budget
controls for difficult graphs
}
}
\examples{
\dontrun{
diffusion <- run_gene_reg_diffusion(
  nodes_path = "gene_reg_graph_scored_nodes.tsv.gz",
  edges_path = "gene_reg_graph_scored_edges.tsv.gz"
)

call_selected_subgraph(
  diffusion = diffusion,
  target_genes = 50L
)
}

}
