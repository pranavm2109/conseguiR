% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{prepare_epigenomic_scores}
\alias{prepare_epigenomic_scores}
\title{Prepare regulatory epigenomic scores}
\usage{
prepare_epigenomic_scores(
  reg_ref_path = NULL,
  track_dir = NULL,
  bw_files = NULL,
  output_path = NULL,
  min_tracks = 3L,
  drop_mhc = TRUE,
  transform = "log1p",
  return_diagnostics = TRUE,
  summary_fun = mean,
  verbose = FALSE
)
}
\arguments{
\item{reg_ref_path}{Optional regulatory-element reference path. When
\code{NULL}, \code{conseguiR} uses its backend-owned ENCODE-derived
regulatory universe.}

\item{track_dir}{Optional directory containing bigWig tracks.}

\item{bw_files}{Optional explicit vector of bigWig paths.}

\item{output_path}{Optional output path for saved scores.}

\item{min_tracks}{Minimum number of tracks required.}

\item{drop_mhc}{Whether to exclude extended MHC elements.}

\item{transform}{Signal transformation method.}

\item{return_diagnostics}{Whether to return the diagnostic table and raw
matrix alongside z scores.}

\item{summary_fun}{Summary function applied to each bigWig over each
regulatory element.}

\item{verbose}{Logical scalar. If \code{TRUE}, show a progress bar and stage
messages during epigenomic scoring.}
}
\value{
An epigenomic score bundle.
}
\description{
Prepare regulatory epigenomic scores
}
\details{
Exact formatting:
\itemize{
\item \code{track_dir}: a single directory path containing bigWig files.
\item \code{bw_files}: a character vector of bigWig paths. Supply at least three
tracks, for example
\verb{c(\\"sample1.bw\\", \\"sample2.bw\\", \\"sample3.bw\\")}.
\item \code{summary_fun}: a function object such as \code{mean} or \code{max}.
}

The current epigenomic score is not a generic "activity" score. Instead,
conseguiR quantifies each regulatory element across the supplied tracks,
summarizes each track over each element with \code{summary_fun}, and then scores
elements by how variable they are across tracks. The intuition is that
highly variable elements are more context-specific, whereas uniformly active
elements are more housekeeping-like.

In practice:
\itemize{
\item \code{transform = "log1p"} is a reasonable default for signal stabilization
\item \code{summary_fun = mean} is a reasonable default for average signal over each
regulatory element
\item at least three tracks are required so that cross-track variability is
meaningful
}
}
\examples{
names(formals(prepare_epigenomic_scores))

}
