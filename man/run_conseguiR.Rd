% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{run_conseguiR}
\alias{run_conseguiR}
\title{Run the full conseguiR pipeline end to end}
\usage{
run_conseguiR(
  gwas_sumstats,
  somatic_maf,
  reg_ref_path = NULL,
  reference_bfile,
  dndscv_refdb,
  epigenomic_track_dir = NULL,
  epigenomic_tracks = NULL,
  graph_rds_path = NULL,
  gg_nodes_path = NULL,
  gg_edges_path = NULL,
  output_dir = NULL,
  target_genes = 50L,
  germline_args = list(),
  somatic_args = list(),
  epigenomic_args = list(),
  scored_graph_args = list(),
  diffusion_args = list(),
  subgraph_args = list(),
  plot_args = list(),
  verbose = FALSE
)
}
\arguments{
\item{gwas_sumstats}{GWAS summary statistics path or table.}

\item{somatic_maf}{Somatic MAF path or table.}

\item{reg_ref_path}{Optional regulatory-element reference path. When
\code{NULL}, \code{conseguiR} uses its backend-owned ENCODE-derived
regulatory universe.}

\item{reference_bfile}{PLINK reference prefix for MAGMA.}

\item{dndscv_refdb}{dndscv reference database path.}

\item{epigenomic_track_dir}{Optional directory containing epigenomic tracks.}

\item{epigenomic_tracks}{Optional explicit vector of bigWig paths.}

\item{graph_rds_path}{Backend no-score gene-reg graph path.}

\item{gg_nodes_path}{Gene-gene node table path.}

\item{gg_edges_path}{Gene-gene edge table path.}

\item{output_dir}{Optional output directory for saved pipeline artifacts.
When \code{NULL}, \code{run_conseguiR()} runs in object-first mode and returns the
stage bundles without treating disk output as the default interface.}

\item{target_genes}{Requested selected-subgraph size.}

\item{germline_args}{Named list of overrides passed to
\code{prepare_germline_scores()}. This list may contain both gene- and
regulatory-run settings, for example \code{gene_sample_size},
\code{reg_sample_size}, \code{gene_step1_args}, \code{gene_step2_args},
\code{reg_step1_args}, \code{reg_step2_args}, and \code{shared_args}.}

\item{somatic_args}{Named list of overrides passed to
\code{prepare_somatic_scores()}. This list may contain both dndscv and fishHook
settings, for example \code{gene_cv},
\code{gene_max_muts_per_gene_per_sample},
\code{gene_max_coding_muts_per_sample}, \code{dndscv_args}, \code{eligible_gr},
\code{fishhook_covariates}, \code{fishhook_covariate_data}, \code{fishhook_idcol}, and
\code{fishhook_args}.}

\item{epigenomic_args}{Named list of overrides passed to
\code{prepare_epigenomic_scores()}. Typical entries include \code{track_dir},
\code{bw_files}, \code{min_tracks}, \code{drop_mhc}, \code{transform},
\code{return_diagnostics}, and \code{summary_fun}.}

\item{scored_graph_args}{Named list of overrides passed to
\code{build_scored_gene_reg_graph()}.}

\item{diffusion_args}{Named list of overrides passed to
\code{run_gene_reg_diffusion()}.}

\item{subgraph_args}{Named list of overrides passed to
\code{call_selected_subgraph()}.}

\item{plot_args}{Named list of overrides passed to
\code{plot_selected_subgraph()}.}

\item{verbose}{Logical scalar. If \code{TRUE}, show progress bars and stage
output across the pipeline.}
}
\value{
A pipeline bundle containing all stage bundles.
}
\description{
Run the full conseguiR pipeline end to end
}
\details{
\code{run_conseguiR()} is a thin orchestration wrapper. Most tuning is passed
through stage-specific named lists:
\itemize{
\item \code{germline_args} controls the two MAGMA branches inside
\code{prepare_germline_scores()}
\item \code{somatic_args} controls the dndscv and fishHook branches inside
\code{prepare_somatic_scores()}
\item \code{epigenomic_args} controls \code{prepare_epigenomic_scores()}
\item \code{scored_graph_args}, \code{diffusion_args}, \code{subgraph_args}, and \code{plot_args}
are forwarded to their corresponding downstream stages
}

Formatting notes:
\itemize{
\item each \verb{*_args} input should be a named list
\item nested MAGMA settings belong inside \code{gene_step1_args}, \code{gene_step2_args},
\code{reg_step1_args}, and \code{reg_step2_args}
\item fishHook covariate-bearing inputs still follow the same rules as
\code{prepare_somatic_scores()}: one row per regulatory element in
\code{fishhook_covariate_data}, and a preformatted fishHook-ready object in
\code{fishhook_covariates} if supplied
}

The gene and regulatory location resources are backend-managed by the
package and are not user-facing arguments in this high-level wrapper.

\code{conseguiR} now uses an object-first design. In plain terms, that means the
compute stages return R objects/bundles by default, and file writing is
optional rather than being the main API. If you supply \code{output_dir},
\code{run_conseguiR()} will save stage artifacts there; if you leave
\code{output_dir = NULL}, it will still run the full workflow and return the
resulting objects in memory.

Exact list formatting:

\verb{germline_args = list( gene_sample_size = 456348, reg_sample_size = 456348, gene_step1_args = list(annotation_window = c(15, 15), ignore_strand = TRUE), gene_step2_args = list( gene_model = \\"snp-wise=mean\\", pval = list(use = c(\\"SNP\\", \\"P\\"), duplicate = \\"drop\\") ), reg_step1_args = list(annotation_window = c(15, 15)), reg_step2_args = list( pval = list(use = c(\\"SNP\\", \\"P\\"), duplicate = \\"drop\\") ), shared_args = list() )}

\verb{somatic_args = list( gene_cv = NULL, sm = \\"192r_3w\\", kc = \\"cgc81\\", eligible_gr = NULL, fishhook_covariate_data = covariate_dt, constructor_use_local_mut_density = FALSE, score_iter = 25L, fishhook_args = list() )}

\verb{epigenomic_args = list( bw_files = c(\\"track1.bw\\", \\"track2.bw\\", \\"track3.bw\\"), min_tracks = 3L, transform = \\"log1p\\" )}
}
\examples{
names(formals(run_conseguiR))

}
