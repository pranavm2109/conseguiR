% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{run_somatic_regulatory_scoring}
\alias{run_somatic_regulatory_scoring}
\title{Run fishHook somatic regulatory scoring}
\usage{
run_somatic_regulatory_scoring(
  maf,
  reg_ref_path = NULL,
  output_path = NULL,
  eligible_gr = NULL,
  fishhook_covariates = NULL,
  fishhook_covariate_data = NULL,
  idcol = "Tumor_Sample_Barcode",
  constructor_out_path = NULL,
  constructor_use_local_mut_density = FALSE,
  constructor_local_mut_density_bin = 1e+06,
  constructor_mc_cores = 1L,
  constructor_na_rm = TRUE,
  constructor_pad = 0,
  constructor_max_slice = 1e+05,
  constructor_ff_chunk = 1e+06,
  constructor_max_chunk = 1e+12,
  constructor_idcap = 1,
  constructor_weight_events = FALSE,
  constructor_nb = TRUE,
  score_sets = NULL,
  score_model = NULL,
  score_return_model = TRUE,
  score_nb = NULL,
  score_iter = NULL,
  score_subsample = NULL,
  score_seed = NULL,
  score_verbose = NULL,
  score_mc_cores = NULL,
  score_p_randomized = NULL,
  score_class_return = TRUE,
  fishhook_args = list(),
  verbose = FALSE
)
}
\arguments{
\item{maf}{Somatic MAF path or table.}

\item{reg_ref_path}{Optional regulatory-element reference path. When
\code{NULL}, \code{conseguiR} uses its backend-owned ENCODE-derived
regulatory universe.}

\item{output_path}{Optional output path for saved scores.}

\item{eligible_gr}{Optional fishHook eligible territory.}

\item{fishhook_covariates}{Optional fishHook covariate objects/specifications.}

\item{fishhook_covariate_data}{Optional tabular covariate data.}

\item{idcol}{Sample identifier column for fishHook.}

\item{constructor_out_path}{Optional fishHook constructor output path.}

\item{constructor_use_local_mut_density}{Whether fishHook should add the
local-mutation-density constructor branch.}

\item{constructor_local_mut_density_bin}{Bin size for fishHook local mutation
density.}

\item{constructor_mc_cores}{fishHook constructor parallel worker count.}

\item{constructor_na_rm}{Whether fishHook constructor covariates drop \code{NA}.}

\item{constructor_pad}{fishHook constructor padding.}

\item{constructor_max_slice}{fishHook constructor \code{max.slice}.}

\item{constructor_ff_chunk}{fishHook constructor \code{ff.chunk}.}

\item{constructor_max_chunk}{fishHook constructor \code{max.chunk}.}

\item{constructor_idcap}{fishHook constructor \code{idcap}.}

\item{constructor_weight_events}{Whether fishHook constructor should weight
events.}

\item{constructor_nb}{fishHook constructor negative-binomial switch.}

\item{score_sets}{Optional sets passed to \code{fishHook::score.hypotheses()}.}

\item{score_model}{Optional score model passed to fishHook.}

\item{score_return_model}{Whether to request the fishHook fitted model.}

\item{score_nb}{Optional score-side negative-binomial switch.}

\item{score_iter}{Optional score-side maximum iteration count.}

\item{score_subsample}{Optional fishHook score subsample size.}

\item{score_seed}{Optional fishHook score seed.}

\item{score_verbose}{Optional fishHook score verbosity override.}

\item{score_mc_cores}{Optional fishHook score parallel worker count.}

\item{score_p_randomized}{Optional fishHook randomized-p setting.}

\item{score_class_return}{Whether fishHook should return the classed result
object.}

\item{fishhook_args}{Named list of additional fishHook scoring arguments.}

\item{verbose}{Logical scalar. If \code{TRUE}, show stage messages and fishHook
output.}
}
\value{
A somatic regulatory score bundle.
}
\description{
This wrapper exposes the relevant fishHook constructor and
\code{score.hypotheses()} parameter surfaces used by the package, while still
allowing additional passthrough through \code{fishhook_args}.
}
\details{
Exact formatting:
\itemize{
\item \code{reg_ref_path}: a path to the regulatory-element reference file.
\item \code{eligible_gr}: a \code{GRanges} object or \code{NULL}.
\item \code{fishhook_covariates}: the covariate specification object expected by the
internal fishHook runner, or \code{NULL}.
\item \code{fishhook_covariate_data}: a data frame/data.table containing one row per
regulatory element and the covariate columns needed by fishHook. In
practice this should include a regulatory-element identifier column plus
the numeric or categorical covariates you want fishHook to model.
\item \code{idcol}: the sample identifier column used by the regulatory somatic input
handed to fishHook. It should match the sample identifier field in the MAF
after harmonization.
\item \code{fishhook_args}: a named list of additional fishHook arguments.
\item \code{reg_ref_path} should resolve to the same regulatory universe you want to
score. In practice this means the regulatory-element IDs in
\code{fishhook_covariate_data} should correspond to the IDs in that reference.
}

In plain language:
\itemize{
\item \code{eligible_gr} defines the territory in which fishHook is allowed to place
mutations when estimating the background model.
\item \code{fishhook_covariate_data} is where users typically supply replication,
accessibility, mappability, or GC-like covariates if they have them.
\item when fishHook returns directional significance columns, \code{conseguiR}
extracts regulatory scores from enrichment-side \code{p} and depletion-side
\code{p.neg} directly rather than reconstructing direction later from a generic
p-value.
\item if you do not have a custom territory or covariates yet, \code{eligible_gr = NULL} and \code{fishhook_covariate_data = NULL} are reasonable starting points.
\item \code{idcol} should match the sample identifier column name used in the somatic
mutation table after harmonization. In most MAF-based workflows the
default \code{Tumor_Sample_Barcode} is the right choice.
}

Minimal covariate-data example:

\verb{covariate_dt = data.frame( reg_elem_id = c(\\"EH38E0080197\\", \\"EH38E2084302\\"), accessibility = c(1.2, 0.4), replication_timing = c(0.7, -0.1), gc_content = c(0.44, 0.51) )}

\code{fishhook_covariates = NULL}

\code{fishhook_args = list()}

A good first-pass mental model is:
\itemize{
\item \code{eligible_gr} says where mutations are allowed to occur in the background
model
\item \code{fishhook_covariate_data} says which feature-level covariates explain that
background
\item \code{fishhook_covariates} is only needed when you already have a fishHook-side
covariate specification you want to pass through directly
}

fishHook documentation:
\url{https://rdrr.io/github/mskilab/fish.hook/man/FishHook.html}

fishHook tutorial:
\url{https://mskilab.com/fishHook/tutorial.html}
}
\examples{
names(formals(run_somatic_regulatory_scoring))

}
