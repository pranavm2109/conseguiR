% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{run_somatic_regulatory_scoring}
\alias{run_somatic_regulatory_scoring}
\title{Run fishHook somatic regulatory scoring}
\usage{
run_somatic_regulatory_scoring(
  maf,
  reg_ref_path,
  output_path = NULL,
  eligible_gr = NULL,
  fishhook_covariates = NULL,
  fishhook_covariate_data = NULL,
  idcol = "Tumor_Sample_Barcode",
  fishhook_args = list(),
  verbose = FALSE
)
}
\arguments{
\item{maf}{Somatic MAF path or table.}

\item{reg_ref_path}{Regulatory-element reference path.}

\item{output_path}{Optional output path for saved scores.}

\item{eligible_gr}{Optional fishHook eligible territory.}

\item{fishhook_covariates}{Optional fishHook covariate objects/specifications.}

\item{fishhook_covariate_data}{Optional tabular covariate data.}

\item{idcol}{Sample identifier column for fishHook.}

\item{fishhook_args}{Named list of additional fishHook scoring arguments.}

\item{verbose}{Logical scalar. If \code{TRUE}, show stage messages and fishHook
output.}
}
\value{
A somatic regulatory score bundle.
}
\description{
This wrapper exposes the main fishHook configuration surface used by the
package workflow and leaves less common model tuning available through
\code{fishhook_args}.
}
\details{
Exact formatting:
\itemize{
\item \code{reg_ref_path}: a path to the regulatory-element reference file.
\item \code{eligible_gr}: a \code{GRanges} object or \code{NULL}.
\item \code{fishhook_covariates}: the covariate specification object expected by the
internal fishHook runner, or \code{NULL}.
\item \code{fishhook_covariate_data}: a data frame/data.table containing one row per
regulatory element and the covariate columns needed by fishHook. In
practice this should include a regulatory-element identifier column plus
the numeric or categorical covariates you want fishHook to model.
\item \code{idcol}: the sample identifier column used by the regulatory somatic input
handed to fishHook. It should match the sample identifier field in the MAF
after harmonization.
\item \code{fishhook_args}: a named list of additional fishHook arguments.
\item \code{reg_ref_path} should resolve to the same regulatory universe you want to
score. In practice this means the regulatory-element IDs in
\code{fishhook_covariate_data} should correspond to the IDs in that reference.
}

In plain language:
\itemize{
\item \code{eligible_gr} defines the territory in which fishHook is allowed to place
mutations when estimating the background model.
\item \code{fishhook_covariate_data} is where users typically supply replication,
accessibility, mappability, or GC-like covariates if they have them.
\item if you do not have a custom territory or covariates yet, \code{eligible_gr = NULL} and \code{fishhook_covariate_data = NULL} are reasonable starting points.
\item \code{idcol} should match the sample identifier column name used in the somatic
mutation table after harmonization. In most MAF-based workflows the
default \code{Tumor_Sample_Barcode} is the right choice.
}

Minimal covariate-data example:

\verb{covariate_dt = data.frame( reg_elem_id = c(\\"GH01J000001\\", \\"GH01J000002\\"), accessibility = c(1.2, 0.4), replication_timing = c(0.7, -0.1), gc_content = c(0.44, 0.51) )}

\code{fishhook_covariates = NULL}

\code{fishhook_args = list()}

A good first-pass mental model is:
\itemize{
\item \code{eligible_gr} says where mutations are allowed to occur in the background
model
\item \code{fishhook_covariate_data} says which feature-level covariates explain that
background
\item \code{fishhook_covariates} is only needed when you already have a fishHook-side
covariate specification you want to pass through directly
}

fishHook documentation:
\url{https://rdrr.io/github/mskilab/fish.hook/man/FishHook.html}

fishHook tutorial:
\url{https://mskilab.com/fishHook/tutorial.html}
}
\examples{
names(formals(run_somatic_regulatory_scoring))

\dontrun{
run_somatic_regulatory_scoring(
  maf = "study_somatic.maf",
  reg_ref_path = "regulatory_elements.loc",
  fishhook_covariate_data = covariate_dt,
  fishhook_args = list()
)
}

}
