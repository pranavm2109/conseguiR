% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_api.R
\name{plot_locus_context}
\alias{plot_locus_context}
\title{Plot a locus-centered multimodal context panel}
\usage{
plot_locus_context(
  chromosome,
  start,
  end,
  postdiff_gene_reg_graph = NULL,
  scored_graph = NULL,
  diffusion = NULL,
  selected_subgraph = NULL,
  nodes_path = NULL,
  edges_path = NULL,
  diffusion_path = NULL,
  selected_nodes_path = NULL,
  label_features = NULL,
  gwas_sumstats = NULL,
  label_top_gwas_snp = FALSE,
  rsid_pmid = NULL,
  label_top_lit_snps = 0L,
  pmid_query = NULL,
  pmid_page_size = 1000L,
  plot_file_path = NULL,
  title = NULL,
  width = 14,
  height = 9,
  dpi = 300,
  save_plot = !is.null(plot_file_path),
  verbose = FALSE
)
}
\arguments{
\item{chromosome}{Locus chromosome, for example \code{"8"} or \code{"chr8"}.}

\item{start}{Locus start coordinate.}

\item{end}{Locus end coordinate.}

\item{postdiff_gene_reg_graph}{Optional merged post-diffusion gene-reg graph
object or \code{.rds} path containing node and edge tables.}

\item{scored_graph}{Optional scored gene-reg graph bundle.}

\item{diffusion}{Optional diffusion bundle.}

\item{selected_subgraph}{Optional selected-subgraph bundle used to highlight
genes and filter displayed regulatory arcs.}

\item{nodes_path}{Optional explicit scored node-table path.}

\item{edges_path}{Optional explicit scored edge-table path.}

\item{diffusion_path}{Optional explicit diffusion table path.}

\item{selected_nodes_path}{Optional explicit selected-subgraph node-table
path.}

\item{label_features}{Optional gene symbols to label. For regulatory-element
labels, the plot uses the top-scoring regulatory element for each requested
gene label within the locus.}

\item{gwas_sumstats}{Optional GWAS summary statistics path or table used for
locus SNP labeling.}

\item{label_top_gwas_snp}{Logical scalar. If \code{TRUE}, label the top GWAS SNP
inside the top germline regulatory element in the locus.}

\item{rsid_pmid}{Optional rsID-to-PMID evidence table with at least
\code{rsid} and \code{pmid} columns.}

\item{label_top_lit_snps}{Integer count of literature-backed SNPs to label.
SNPs are restricted to regulatory elements in the locus. If no
literature-backed SNPs survive the lookup/filtering steps, \code{conseguiR}
falls back to top GWAS SNPs from the top germline regulatory elements.}

\item{pmid_query}{Deprecated. This argument is currently ignored; locus SNP
labeling now uses dbSNP citation support directly.}

\item{pmid_page_size}{Maximum number of PMIDs per rsID retained from the
dbSNP citation lookup.}

\item{plot_file_path}{Optional output path for the saved figure.}

\item{title}{Plot title.}

\item{width}{Plot width in inches.}

\item{height}{Plot height in inches.}

\item{dpi}{Plot DPI.}

\item{save_plot}{Whether to save the figure.}

\item{verbose}{Logical scalar. If \code{TRUE}, show stage messages.}
}
\value{
A plot bundle containing the ggplot object and locus plotting data.
}
\description{
Creates a genome-track-style panel over a user-specified locus. Genes are
drawn as rectangles, regulatory elements as circles, and regulatory links as
arcs. The panel combines post-diffusion gene scores with locus-matched
regulatory scores from the scored graph.
}
\details{
Exact formatting:
\itemize{
\item \code{chromosome}: a single chromosome string such as \code{"8"} or \code{"chr8"}
\item \code{start}, \code{end}: scalar genomic coordinates defining the locus
\item \code{postdiff_gene_reg_graph}: an optional merged post-diffusion graph object
containing node and edge tables
\item \code{scored_graph}: the bundle returned by \code{build_scored_gene_reg_graph()}
\item \code{diffusion}: the bundle returned by \code{run_gene_reg_diffusion()}
\item \code{selected_subgraph}: the bundle returned by \code{call_selected_subgraph()}
\item \code{label_features}: a character vector of gene symbols such as
\code{c("MYC", "BCL2", "BCL6")}
\item \code{rsid_pmid}: an optional literature-support table with at least
\code{rsid} and \code{pmid} columns
\item \code{label_top_lit_snps}: number of literature-backed SNPs to label before
falling back to top GWAS SNPs in the top germline regulatory elements
}

Track semantics:
\itemize{
\item the top three rows show regulatory-element somatic, epigenomic, and
germline z-scores
\item the \verb{Reg elements} row shows regulatory elements colored by their combined
pre-diffusion norm
\item the bottom gene row shows post-diffusion \code{conseguiR} scores for genes in
the locus
\item thin black diagonal curves connect regulatory elements to genes
\item locus SNP labels prefer literature-backed SNPs when available and
otherwise fall back to top GWAS SNPs in the top germline regulatory
elements
}
}
\examples{
names(formals(plot_locus_context))

}
